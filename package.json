{
  "name": "umc-site",
  "private": true,
  "version": "1.0.0",
  "description": "Uganda Media Centre — Figma-to-web",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "tsc && python scripts/umc-server.py"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
